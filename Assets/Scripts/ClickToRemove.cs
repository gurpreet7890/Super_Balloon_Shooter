using UnityEngine;

public class ClickToRemove : MonoBehaviour
{
    void Update()
    {
    //     if (Input.GetMouseButtonDown(0))
    //     {
    //         // Convert screen position to world point
    //         Vector2 mousePos = Camera.main.ScreenToWorldPoint(Input.mousePosition);

    //         // Perform a 2D raycast
    //         RaycastHit2D hit = Physics2D.Raycast(mousePos, Vector2.zero);

    //         // If hit, call Pop method
    //         if (hit.collider != null)
    //         {
    //             Balloon balloon = hit.collider.GetComponent<Balloon>();
    //             if (balloon != null)
    //             {
    //                 balloon.Pop(); // Call the Pop method instead of deactivating directly
    //             }
    //         }
    //     }
    }
}
